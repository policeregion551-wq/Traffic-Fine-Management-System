{
  "entities": {
    "User": {
      "title": "User",
      "description": "System user (Police, Driver, or Admin)",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["police", "driver", "admin"] },
        "region": { "type": "string" },
        "zone": { "type": "string" },
        "city": { "type": "string" },
        "wereda": { "type": "string" }
      },
      "required": ["uid", "email", "role"]
    },
    "Fine": {
      "title": "Fine",
      "description": "A traffic fine issued to a driver",
      "type": "object",
      "properties": {
        "driverName": { "type": "string" },
        "driverEmail": { "type": "string", "format": "email" },
        "phoneNumber": { "type": "string" },
        "vehicleType": { "type": "string" },
        "licenseNumber": { "type": "string" },
        "violationType": { "type": "string" },
        "violationLocation": { "type": "string" },
        "violationDate": { "type": "string", "format": "date" },
        "paymentDeadline": { "type": "string", "format": "date" },
        "dailyPenaltyRate": { "type": "number" },
        "recordCode": { "type": "string" },
        "officerEmail": { "type": "string", "format": "email" },
        "officerUid": { "type": "string" },
        "amount": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "paid", "verified", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" },
        "receiptImageUrl": { "type": "string" }
      },
      "required": ["driverName", "driverEmail", "phoneNumber", "licenseNumber", "amount", "status", "officerEmail", "recordCode"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile data"
    },
    "/fines/{fineId}": {
      "schema": "Fine",
      "description": "Traffic fine records"
    }
  }
}
